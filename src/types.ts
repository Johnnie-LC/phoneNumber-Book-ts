export interface Contact {
  firstName: string
  lastName: string
  phoneNumber: string
  id: number | string
}

export interface Placeholder {
  firstName: string
  lastName: string
  phoneNumber: string
}
